(function() {

    "use strict";

    var HeaderController = function(
        $state,
        $rootScope,
        $scope
    ) {


    };

    angular.module(app).controller('HeaderController', [
        '$state',
        '$rootScope',
        '$scope',
        HeaderController
    ]);

}());